import { styled } from "styled-components";

export const StyledHeader = styled.header`
  align-items: center;
  display: flex;
  gap: 5px;
  justify-content: center;
  padding-block: 10px;
  z-index: 1;
`;
